
$word_hook:
  set:
    - word

$id_hook:
  set:
    - id

$num_hook:
  set:
    - num

$char_hook:
  set:
    - char

$all_data:
  set:
    - $num_hook
    - $word_hook
    - $char_hook
    - $id_hook

go:
  $all_data